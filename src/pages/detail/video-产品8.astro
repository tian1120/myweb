---
import DetailPostLayout from 'src/layouts/DetailPostLayout.astro';

const title = '测温学习';
const description = '产品教程';
const keywords = '测温学习, 作品集, 视频展示';
---
<DetailPostLayout title={title} description={description} keywords={keywords}>
  <main>
    <section class="project-intro">
      <h1>{title}</h1>
      <p>{description}</p>
    </section>
    <section class="project-content">
      <h2>视频展示</h2>
      <iframe 
        src="https://player.xinpianchang.com/?aid=13289835&amp;mid=DA96QeeDlGRQj5N0" 
        allowfullscreen 
        allow="fullscreen" 
        frameborder="0"
        class="video-iframe"
      ></iframe>
    </section>
  </main>
</DetailPostLayout>

<style>
  .project-content {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 2rem 0;
    padding: 2rem;
    background: transparent;
    border-radius: 17px;
    box-shadow: none;
    color: white;
  }

  .video-iframe {
    width: 70%; /* 根据需要调整宽度 */
    height: 494px; /* 根据需要调整高度 */
    aspect-ratio: 16 / 9; /* 根据视频的实际宽高比调整 */
    object-fit: cover; /* 或者使用 contain */
    border: none; /* 去掉边框 */
    border-radius: 12px; /* 保留圆角 */
  }

  h2 {
    margin-bottom: 1rem;
    font-size: 1.5rem;
    text-align: center;
    color: white;
  }
</style>